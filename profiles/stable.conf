TARGET=run          # worktree name under ~/Developer/calibre-web/; 'none' = don't deploy
BRANCH=run/stable   # result branch name

BRANCHES=(
    bug/kobo-sync
    # bug/kobo-mode-switch   # promote to stable when ready
)

TARGET=lab          # worktree name under ~/Developer/calibre-web/; 'none' = don't deploy
BRANCH=run/canary   # result branch name

BRANCHES=(
    bug/kobo-sync
    # bug/kobo-mode-switch
    # feat/series-author-add-shelf
)

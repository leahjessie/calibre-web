TARGET=none         # deploy explicitly with --target run or --target lab
BRANCH=run/canary   # result branch name

BRANCHES=(
    bug/kobo-sync
    bug/kobo-priority-timestamp
    # bug/kobo-mode-switch
    # feat/series-author-add-shelf
)

[pytest]
testpaths = tests
python_files = test_*.py
# test/ (singular) is where upstream's selenium tests land when run against
# this repo. Exclude it so those don't get picked up by the fast unit test run.
norecursedirs = test
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning:sqlalchemy
